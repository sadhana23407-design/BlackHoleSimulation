#pragma once

#include <glad/glad.h>
#include <GLFW/glfw3.h>

class Application
{
public:
    Application();
    ~Application();

    bool Initialize();
    void Run();
    void Shutdown();

private:
    GLFWwindow* window;

    const unsigned int WIDTH = 1280;
    const unsigned int HEIGHT = 720;
};